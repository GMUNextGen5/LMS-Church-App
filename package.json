{
  "name": "lms-firebase",
  "version": "1.0.0",
  "description": "Firebase-hosted Learning Management System with 3-tier access control",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "@google/generative-ai": "^0.17.1",
    "dompurify": "^3.3.1",
    "firebase": "^10.13.0"
  },
  "devDependencies": {
    "@types/dompurify": "^3.0.5",
    "@types/node": "^22.5.0",
    "typescript": "^5.5.4",
    "vite": "^7.3.1"
  }
}
