/**
 * Shared debounce window (ms) for search filters, account dropdown search, AI chat mirror,
 * password strength meters, and related MutationObserver coalescing.
 */
export const LMS_SEARCH_DEBOUNCE_MS = 75;
