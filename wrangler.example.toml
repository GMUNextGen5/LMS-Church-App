# Same as committed `wrangler.toml` — kept for local experiments / copy-paste.
# For `wrangler pages dev`, the committed root `wrangler.toml` is enough.

name = "lms-church-app"
pages_build_output_dir = "./dist"
compatibility_date = "2026-01-24"
