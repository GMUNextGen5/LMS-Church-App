# Dependencies
node_modules/
firebase-functions/node_modules/

# Build outputs
dist/
firebase-functions/lib/

# Cloudflare Wrangler (local state; never commit)
.wrangler/
# Root `wrangler.toml` is gitignored so Cloudflare Pages Git builds do not load
# the Wrangler v2 BETA path that fails after config validation. See
# `wrangler.example.toml` and DEPLOYMENT.md.
wrangler.toml
coverage/
firebase-functions/coverage/

# Environment variables (never commit secrets)
.env
.env.local
.env.*.local
.env.development
.env.production
firebase-functions/.env

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
debug-ecf1fb.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo

# Temporary commit message file
.commitmsg

# Prettier scratch logs (generated by local CI dry-runs)
prettier.log

# Imported reference-only source package (ported into firebase-functions/ + src/)
AI FEATUES/
